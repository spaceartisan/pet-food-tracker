# pet-food-tracker

Track how much your pet eats per day, including bowl measurements, treats, and counted kibble.

The app runs entirely in the browser and is suitable for GitHub Pages. Data is stored locally in the browser; use **Download backup** for JSON backups. **Vet PDF report** builds a configurable printable report that can be saved as PDF from the browser's print dialog.
